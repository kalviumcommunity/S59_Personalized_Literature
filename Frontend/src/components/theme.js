import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#14110e',
    },
    secondary: {
      main: '#bf6700',
    },
    background: {
      default: '#ffb184',
    },
  },
});

export default theme;
